version: '3'

services:
  web:
    build: .
    ports:
      - "80:8000"  # Map port 80 to container port 8000 for standard HTTP access
    volumes:
      - ./Experiment:/app/Experiment
    environment:
      - FLASK_APP=server.py
      - FLASK_ENV=production
      - PORT=8000
    restart: always